require "spec_helper"

RSpec.describe Omniauth::Honeywell do
  it "has a version number" do
    expect(Omniauth::Honeywell::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
